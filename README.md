Executando banco de dados via docker compose:
Execute o comando no terminal (na mesma pasta do arquivo "docker-compose.yml"):
docker-compose up -d
Parar o banco de dados e o minio:
docker-compose down